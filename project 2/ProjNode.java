// class that should holds data and points to next data

// currently creates node
// TODO: hold data
// TODO: point to next data
public class ProjNode {
    class node<T>{
        T data;
        node<T> next;

        node (T data){
            this.data = data;
            this.next = null;
        }
    }
}
